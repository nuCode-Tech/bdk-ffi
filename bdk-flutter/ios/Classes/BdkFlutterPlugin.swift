import Flutter
import UIKit

public class BdkFlutterPlugin: NSObject, FlutterPlugin {
  public static func register(with registrar: FlutterPluginRegistrar) {
    // No-op: this plugin only provides native binaries for FFI.
  }
}


